import { Box, styled } from "@mui/material";

export const StyledOutlinedIcon = styled(Box)(() => ({
  height: "64px",
  width: "64px",
  border: "5px solid rgba(45, 45, 45, 0.05)",
  borderRadius: "50%",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
}));
